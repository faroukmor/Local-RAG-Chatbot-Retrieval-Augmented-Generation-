Pointers in C are variables that store memory addresses. Instead of holding a value directly, a pointer holds the address where the value is stored. This allows for dynamic memory allocation using functions like malloc and free. Pointers are also essential for passing arguments by reference to functions, modifying the original variables. However, if not managed carefully, pointers can lead to memory leaks or segmentation faults.